name:                magic-typelevel-elem
version:             0.1

ghc-options: -W -Wall -dcore-lint

dependencies:
  - base

library:
  source-dirs: src
  dependencies:
    - magic-tyfams
    - ghc >= 8.6.5 && < 8.7
